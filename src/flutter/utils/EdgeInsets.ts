export type EdgeInsets =
	| { all: number }
	| { x: number }
	| { y: number }
	| { x: number; y: number }
	| { top: number }
	| { bottom: number }
	| { left: number }
	| { right: number }
	| { top: number; bottom: number }
	| { top: number; bottom: number };
